# Where allure-java writes raw result JSON. Kept in sync with the same-named system property
# set by maven-failsafe-plugin, so the location is correct no matter which working directory
# the test JVM is forked in.
allure.results.directory=target/allure-results
